// same as map exccept it stores unique value but does not maintain sorted order
#include <bits/stdc++.h>
using namespace std;
int main()
{
    unordered_map<int,int> um;
    // rest are all the same
    
    return 0;
}