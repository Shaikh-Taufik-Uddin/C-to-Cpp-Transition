// same as set exccept it stores unique value but does not maintain sorted order
#include <bits/stdc++.h>
using namespace std;
int main()
{
    unordered_set<int> us;
    // rest are all the same
    // lower bound and upper bound functions do NOT work here
    return 0;
}