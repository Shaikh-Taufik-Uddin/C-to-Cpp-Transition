// same as map exccept it can store duplicate keys
#include <bits/stdc++.h>
using namespace std;
int main()
{
    multimap<int,int> mp;
    // rest are all the same
    //mp[] function doesnt work here only insert()
    return 0;
}